export const metadata = {
    title: "Code Suggestion Agent",
    description: "Inscription utilisateur simple",
  };
  
  export default function RootLayout({ children }: { children: React.ReactNode }) {
    return (
      <html lang="fr">
        <body>{children}</body>
      </html>
    );
  }
  